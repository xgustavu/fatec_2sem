// function formatar(entrada: string): string {
//   return `<div>${entrada}</div>`;
// }

const f = (entrada: string): string => `<div>${entrada}</div>`;

console.log(f("Ana"));
console.log(f("12"));